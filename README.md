## venv

```sh
# 1. 仮想環境を作成
python -m venv .venv

# 2. 仮想環境をアクティベート
.venv\Scripts\activate.ps1

# 3. pipを最新版に更新
python -m pip install --upgrade pip

# 4. ライブラリをインストール
python -m pip install -r requirements.txt
```
